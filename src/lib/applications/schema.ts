import { z } from "zod";
export const createApplicationSchema=z.object({appliedAt:z.coerce.date(),company:z.string().trim().min(1).max(255),role:z.string().trim().min(1).max(255),roleCategory:z.string().trim().max(120).optional(),seniority:z.string().trim().max(80).optional(),country:z.string().trim().max(120).optional(),workMode:z.string().trim().max(40).optional(),source:z.string().trim().max(80).optional(),vacancyUrl:z.string().url().optional().or(z.literal("")),cvVersion:z.string().trim().max(120).optional(),workAuthorization:z.string().trim().max(120).optional(),salaryMin:z.number().int().nonnegative().optional(),salaryMax:z.number().int().nonnegative().optional(),currency:z.string().trim().max(10).optional(),notes:z.string().optional()});
